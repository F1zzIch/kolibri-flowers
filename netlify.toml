# ВРЕМЕННО: диагностика — пишем вывод сборки в файл и публикуем его.
[build]
  command = "mkdir -p diag && (npm run build > diag/buildlog.txt 2>&1 || true)"
  publish = "diag"

[build.environment]
  NODE_VERSION = "22"
